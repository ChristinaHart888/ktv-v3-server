# ktv-v3-server

### Introduction
This server is used as the backend service for ktv3. This backend server was added to decouple the front end from the database. This also brings forth the ability to add certain new features such as AI vocal isolation.

### API Usage
The API usage from the server is seperated into a number of categories:
1. User API
1. Room API
1. Music API

#### User API
